포함관계 : 한 클래스의 멤버 변수로 다른 클래스 타입의 참조 변수 선언
	(상속과 유사한 개념)

public class Circle {							┌	public class Point {
										포함관계	│
	Point p = new Point();			<==========	│	int x;
	int r;										│	int y;
}												└	}

대표적인 포함관계 : String Class

원은 점이다 : is-a			(상속)
원은 점을 가지고 있다 : has-a	(포함)
